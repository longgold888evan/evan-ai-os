# Qt Graph Algorithms

Goal: visualize a directed graph, adjacency lists, topological sorting with indegree changes, and critical path calculations.

Critical path outputs: Ve, Vl, E, L, and L-E; zero slack indicates a critical activity.
